spring:
  application:
    name: ToDoList

  datasource:
    username: laststap
    url: jdbc:postgresql://localhost:5432/toDoList
    password: lastPasswordForToDoList
    driver-class-name: org.postgresql.Driver
  jpa:
    open-in-view: false

jwt:
  secret: ${JWT_SECRET:e0de953b-72ab-474c-8fb8-bed8a6128ba1}
  ttl-millis: 43200000
